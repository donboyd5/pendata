% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/frs_functions.R
\name{benmult_lookup}
\alias{benmult_lookup}
\title{Look up benefit multiplier using priority-ordered rules}
\usage{
benmult_lookup(data, benefit_multipliers)
}
\arguments{
\item{data}{A data frame with columns \code{class}, \code{tier}, \code{status},
\code{dist_age}, and \code{yos}.  \code{status} should be \code{"norm"} or \code{"early"}.}

\item{benefit_multipliers}{The rule table (typically
\code{frs$benefit_multipliers} from pendata).  Must have columns \code{class},
\code{tier}, \code{status}, \code{priority}, \code{min_yos}, \code{min_age}, \code{max_age},
\code{benmult}.}
}
\value{
A numeric vector of length \code{nrow(data)} with the benefit
multiplier for each row (0 where no rule matches).
}
\description{
Applies the \code{benefit_multipliers} rule table to a data frame of member
records.  Rules are matched in ascending priority order (priority 1 is
checked first); the first matching rule's \code{benmult} is returned.
}
\details{
Matching conditions (all must hold):
\itemize{
\item \code{yos >= min_yos}
\item \code{dist_age >= min_age} (ignored when \code{min_age} is \code{NA})
\item \code{dist_age < max_age}  (ignored when \code{max_age} is \code{NA})
}
}
\examples{
\dontrun{
params$ben_mult_lookup <- tidyr::expand_grid(
  class = unique(params$class_names_no_drop_frs_),
  tier  = c("tier_1", "tier_2", "tier_3"),
  status = c("norm", "early"),
  dist_age = params$age_range_,
  yos      = params$yos_range_
) |>
  dplyr::mutate(
    benmult = benmult_lookup(
      dplyr::pick(class, tier, status, dist_age, yos),
      frs$benefit_multipliers
    )
  )
}

}
